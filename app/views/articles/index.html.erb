<p style="color: green"><%= notice %></p>

<h1>Articles</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Actions</th>
    </tr>
  </thead>

<tbody>
  <% @articles.each do |article| %>
    <td><%= render.title %></td>
    <td><%= render.Description %></td>
    <td>link_to 'Delete', '#'</td>

  <% end %>
</div>

<%= link_to "New article", new_article_path %>
